<template name="inputTemplate">
  <view class="input-container">
    <image class="input-img" src="{{img}}" />
    <image class="input-line" src="{{line}}" />
    <input class="input-content" placeholder="{{content}}"></input>
    <button class="phone-code" wx:if="{{phoneCode}}">获取验证码</button>
  </view>
</template>